### 解题思路

1. 满足左右括号都用完时，加入到结果中
2. 非法情况，如右括号使用大于左括号，或者左右括号数量为负，则直接返回
3. 每次在字符串中加“(”或“)”

### 代码

* golang

```golang
func generateParenthesis(n int) []string {
    var ans []string
    str := ""
    backtrack(&ans, str, n, n)
    return ans
}

func backtrack(ans *[]string, str string, a, b int) {
    if a == 0 && b == 0 { //左右括号都用完了，则添加到结果中
        *ans = append(*ans, str)
        return
    }
    if a < 0 || b < 0 || a > b { //不合法情况，直接返回
        return
    }

    str += "("
    backtrack(ans, str, a-1, b)
    str = str[:len(str)-1]

    str += ")"
    backtrack(ans, str, a, b-1)
    str = str[:len(str)-1]
}
```

