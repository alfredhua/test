spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:4040/
      profile: dev
      label: master
    discovery:
      service-id: config-server

server:
  port: 2001